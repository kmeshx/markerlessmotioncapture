function [startpt, endpt] = reorient2(p1, p2, p3, p4, ref)
    p = [p1; p2; p3; p4];
    d(1) = norm(p1 - ref); d(2) = norm(p2 - ref);
    d(3) = norm(p3 - ref); d(4) = norm(p4 - ref);
    [dmax, i] = max(d);
    
    p
end